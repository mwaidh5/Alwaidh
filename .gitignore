# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Build outputs
dist
dist-ssr
build
*.local

# Environment
.env
.env.local
.env.development.local
.env.production.local

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase
.firebase
firebase-debug.log
firestore-debug.log
ui-debug.log

# Local preview helpers
serve_preview.py
_test_*

# Self-hosted background-removal model (regenerated from node_modules at build)
public/imgly-data/
public/pdf-assets/

# CI-generated Android signing properties — never commit
android/key.properties
*.jks
*.keystore

# Build artefacts. vite.config.js in particular shadows vite.config.ts,
# so a stale copy silently overrides the real config.
vite.config.js
vite.config.d.ts
vite.config.js.timestamp-*.mjs
*.tsbuildinfo

# A separate project that lives in this folder — never part of this repo.
balloon-tasks/
